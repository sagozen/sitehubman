# Frontend

Use this folder for future UI-focused modules if the project moves from feature-first to frontend/backend separation.

Current frontend code still lives in:

- `src/features`
- `src/components`
- `src/design-system`
