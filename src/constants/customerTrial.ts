/** Registered customers get a no-card digital trial after sign-up. */
export const CUSTOMER_TRIAL_DAYS = 7;

/** Anonymous guest preview length (same window for consistency). */
export const GUEST_PREVIEW_TRIAL_DAYS = 7;
